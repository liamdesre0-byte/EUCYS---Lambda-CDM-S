# Documentation

- **Theoretical derivation** — LaTeX source in
  [`../paper/LCDMS_CWSF_Written_Report.tex`](../paper/LCDMS_CWSF_Written_Report.tex)
  and the mathematics notes under [`../mathematics/`](../mathematics/).
  Compile with pdfLaTeX to produce `theoretical_derivation.pdf`.
- **Poster** — export the competition poster PDF here as
  `eucys_poster.pdf` once its numbers are regenerated from
  `output/tables/` (the pre-audit poster quotes superseded values; see the
  integrity statement in the top-level README).

No PDF is committed until its contents match the current pipeline output —
documents and code must never disagree.
